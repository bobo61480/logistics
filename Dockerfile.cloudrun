# Lightweight nginx container serving the Next.js static export.
# Used only by the cloud-run deploy target in cloudbuild.yaml.
FROM nginx:1.27-alpine

COPY out/ /usr/share/nginx/html/
COPY nginx-cloudrun.conf /etc/nginx/conf.d/default.conf

EXPOSE 8080
